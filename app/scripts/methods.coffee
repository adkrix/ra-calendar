$.RaCalendar.methods =

  normalizeEvents: (event) ->
    R = $.RaCalendar.config.events
    H = $.RaCalendar.handlers
    event

  updateEvents: (events) ->
    events

  updateResources: (resources) ->
    resources

